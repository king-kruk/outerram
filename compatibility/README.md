# Empirical compatibility results

`results.json` is intentionally empty until a model/runtime combination passes the physical-Mac validation gate.

Do not add extrapolated or upstream-only benchmark numbers as OuterRAM results. Each row must include exact hardware, macOS, model revision/quantization, strategy, context length, storage path/type, memory/swap, TTFT, decode throughput and tool-call status.
